name: 'rick_and_morty_dbt'
version: '1.0.0'

profile: 'rick_and_morty_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:       
  - "target"
  - "dbt_packages"

models:
  rick_and_morty_dbt:
   
    example:
      +materialized: view
